local colorscheme = {
  'config.colorscheme.dracula',
  'config.colorscheme.one',
  'config.colorscheme.paradox',
  'config.colorscheme.solarized',
}
-- require(colorscheme[os.time() % 4 + 1])
require(colorscheme[1])
